## C语言判断
```c
if (条件成立) {
	...
}
```
- 上述代码是if判断语句的基本语法
	- 表达的是如果条件成立则执行块中的语句
		- 如果条件不成立则跳过执行下面的语句
- 判断条件包含计算两个值之间的关系,叫做关系运算
- 关系运算符
	| 运算符 | 意义 |
	| :---: | :---: |
	| == | 相等 |
	| != | 不相等 |
	| > | 大于 |
	| >= | 大于或等于 |
	| < | 小于 |
	| <= | 小于或等于 |

